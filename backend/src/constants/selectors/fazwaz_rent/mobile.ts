/**
 * From search items
 */
const searchItem = '.result-search__item';
// Include Array images
const imagesContainer = '.embla__container';
const link = '.link-unit';
const title = '.unit-info__description-title';
const location = '.location-unit';

/**
 * From linked item
 */
const description = '.unit-view-description';

const features = '.unit-features';
// Include image + text
const feature = '.unit-features__item';

const basicInforms = '.basic-information';
const basicInfoItem = '.basic-information__item';
const basicInfoTopic = '.basic-information-topic';
const basicInfo = '.basic-information-info';

const availableNow = '.available_now';

const price = '.header-detail-page-price__rate';

const projectHighlight = '.project-highlight';
const projectHighlightTitle = '.highlight__title';

const photos = '.swiper-container.instance-1 > .swiper-wrapper';
const photo = '.img-swipe';

const contact = '.ask-a-question__btn';

export const fazwazRentMobileSelectors = {
  searchItem,
  imagesContainer,
  link,
  price,
  title,
  location,
  description,
  features,
  feature,
  basicInfo,
  basicInfoTopic,
  basicInforms,
  basicInfoItem,
  availableNow,
  projectHighlight,
  projectHighlightTitle,
  photos,
  photo,
  contact,
};
